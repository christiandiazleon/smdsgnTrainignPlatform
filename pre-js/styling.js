$(document).ready(function($) {
    
    // registrar el cambio de tamaño de la pantalla
    $(window).resize(function () {
        if ($(window).width() > 200) {

        } else {

        }
    });

});